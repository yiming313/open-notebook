"""Command-line interface for Open Notebook.

Provides a thin HTTP client over the running Open Notebook API so external
callers (e.g. coding agents) can ask scoped questions against a single source
or an entire notebook and receive a distilled text answer on stdout.
"""
